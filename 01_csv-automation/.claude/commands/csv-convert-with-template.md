---
description: 保存済テンプレートを適用してCSVを一発変換
---

引数は `<入力CSV> <テンプレート名>` の順で与えられます。
`python3 src/main.py convert --input <入力CSV> --template <テンプレート名>` を実行し、翌月分などの再利用シナリオで数秒で変換が完了することを強調しつつ、出力ファイルとレポートのパスを示してください。
